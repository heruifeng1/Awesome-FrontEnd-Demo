describe('', function () {
    it('should 无参数创建类,默认拥有空listeners', function () {
        // give
            const eventEmitter = new EventEmitter();
        // when

        // then
        expect(eventEmitter.getListeners).toEqual([])
    });
    it('should ', function () {
        
    });
});
